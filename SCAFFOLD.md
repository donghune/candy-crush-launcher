# Scaffold trail

Generated by `@bitstone/create-launcher` on 2026-05-17T03:54:14.590Z.
Base launcher: `/Users/yudonghun/IdeaProjects/blue-zone-launcher`

## Answers
```json
{
  "launcherId": "candy-crush-launcher",
  "productName": "Candy Crush Launcher",
  "appId": "candycrushlauncher",
  "description": "Candy Crush Minecraft Launcher",
  "author": "donghune",
  "homepage": "https://github.com/donghune/candy-crush-launcher",
  "githubUser": "donghune",
  "githubRepo": "candy-crush-launcher",
  "copyrightYear": 2026,
  "server": {
    "id": "candy-crush-main",
    "name": "Candy Crush Server",
    "description": "Candy Crush event - Fabric 1.21.4",
    "address": "play.example.com:25565",
    "minecraftVersion": "1.21.4",
    "icon": "https://raw.githubusercontent.com/donghune/candy-crush-launcher/master/app/assets/images/SealCircle.png"
  },
  "modulePresets": [
    "fabric-1.21.4",
    "fabric-api-0.119.4+1.21.4",
    "fabric-language-kotlin-1.13.11",
    "sodium-iris",
    "complementary-r5.7.1"
  ],
  "extraModules": [
    {
      "id": "kr.donghune:candy_crush:1.0-SNAPSHOT",
      "name": "Candy Crush Mod",
      "type": "FabricMod",
      "artifact": {
        "size": 0,
        "MD5": "",
        "url": "https://github.com/donghune/candy-crush-launcher/releases/download/v1.0.0/candy_crush-1.0-SNAPSHOT.jar"
      }
    }
  ]
}
```

## Next steps
1. Replace branding assets under `app/assets/images/` (logo, SealCircle.png) so they match `Candy Crush Launcher`.
2. Review `distribution.json` — mod jars referenced via GitHub Releases need real `size`/`MD5`.
3. Inspect the residuals listed below and edit by hand where needed.
4. `npm install && npm start` to dev-launch.

## Residuals from the base launcher (review by hand)

- `app/assets/lang/_custom.toml` — Blue Zone (literal)
- `app/assets/lang/_custom.toml` — BLUE ZONE (uppercase)
- `app/assets/lang/ko_KR.toml` — Blue Zone (literal)
- `distribution.json` — blue-zone-launcher (URL/path)

Notes:
- Game-specific lines (e.g. `welcomeHeader`, `welcomeDescription`, gameplay strings in `app/assets/lang/*.toml`) are intentionally left intact — they describe the *event*, not the launcher, so they need a manual rewrite for each new event.
- Shader/file URLs in `distribution.json` still point at the base launcher's GitHub Release. After uploading the same assets to `donghune/candy-crush-launcher` releases, update those URLs.
